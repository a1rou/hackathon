# AI-сервис предварительной оценки ювелирных изделий

Прототип сервиса для предварительной дистанционной оценки золотых ювелирных изделий по фотографиям и данным анкеты клиента.

Проект разработан в рамках хакатона для кейса компании **ООО «СКС Ломбард»**.

## Описание проекта

Сервис позволяет клиенту загрузить фотографии ювелирного изделия, указать базовые параметры и получить предварительный расчёт:

- суммы займа;
- суммы выкупа;
- вероятности принятия изделия в залог;
- комментариев по состоянию изделия и возможным дефектам.

Основная цель проекта — дать клиенту предварительное понимание возможной оценки изделия до посещения подразделения ломбарда и сформировать дополнительный поток обращений в цифровом канале.

## Решаемая проблема

До внедрения такого сервиса клиенту необходимо лично прийти в подразделение компании, чтобы товаровед выполнил оценку изделия. Часть потенциальных клиентов не готова посещать офис без понимания примерной суммы займа или выкупа.

MVP решает эту проблему за счёт онлайн-оценки с использованием мультимодальной языковой модели и простой формы конвертации результата в обращение.

## Возможности MVP

- загрузка 1–4 фотографий изделия;
- ввод анкеты изделия:
  - тип изделия;
  - проба металла;
  - наличие вставок;
  - состояние;
  - вес изделия, если известен;
- анализ фотографии с помощью ИИ;
- определение:
  - типа изделия;
  - состояния;
  - наличия вставок;
  - наличия дефектов;
  - повреждения вставок;
  - примерного веса изделия, если вес не указан;
- расчёт предварительной суммы займа;
- расчёт предварительной суммы выкупа;
- оценка вероятности принятия изделия в залог;
- предупреждение пользователя о предварительном характере расчёта;
- создание заявки через заглушку интеграции с Битрикс24;
- выбор подразделения или заказ обратного звонка.

## Область применения

На этапе MVP сервис работает с:

- золотыми изделиями;
- ювелирными украшениями;
- пробами золота: `375`, `585`, `750`;
- типами изделий:
  - кольцо;
  - серьги;
  - браслет;
  - кулон;
  - цепь;
  - колье.

## Используемая модель ИИ

Для анализа фотографий используется мультимодальная языковая модель:

**Qwen3.6-35B (FP8)** из **Yandex Cloud AI Studio**.

Модель анализирует фотографии изделия и возвращает структурированный JSON с признаками изделия, состоянием, дефектами и приблизительной оценкой веса.

## Технологический стек

- Python
- Streamlit
- OpenAI-compatible API client
- Yandex Cloud AI Studio
- python-dotenv
- Битрикс24 REST API, в MVP реализована заглушка интеграции

## Структура проекта

```text
ai-lombard/
├── .env
├── .gitignore
├── analyzer.py
├── app.py
├── bitrix.py
├── config.py
├── pricing.py
├── README.md
└── requirements.txt

Назначение файлов

app.py — основной Streamlit-интерфейс приложения.
analyzer.py — анализ фотографий изделия с помощью ИИ и нормализация результата.
pricing.py — тарифные сетки, расчёт стоимости и вероятности принятия.
bitrix.py — заглушка создания сделки в Битрикс24.
config.py — загрузка переменных окружения и настройка модели.
requirements.txt — зависимости проекта.
.env — локальные переменные окружения.

Логика работы сервиса

1.
Клиент загружает фотографии изделия и заполняет анкету.
2.
Сервис передаёт изображения и промпт в мультимодальную модель.
3.
ИИ возвращает JSON с анализом изделия.
4.
Сервис нормализует результат анализа.
5.
На основании пробы, веса и состояния рассчитывается предварительная сумма займа.
6.
По дефектам, состоянию, вставкам и весу рассчитывается вероятность принятия.
7.
Пользователь видит результат и предупреждение о предварительном характере оценки.
8.
Пользователь может:
выбрать подразделение;
заказать обратный звонок.
9.
После заполнения контактов создаётся сделка через модуль интеграции с Битрикс24.

Предупреждение о предварительной оценке

Расчёт является предварительным и выполняется на основании фотографий и предоставленных пользователем данных.


Окончательная оценка изделия определяется специалистом после очного осмотра в подразделении компании. Итоговые суммы займа и выкупа могут отличаться от расчёта в сервисе.

Установка и запуск

1. Клонировать репозиторий

git clone <URL_РЕПОЗИТОРИЯ>
cd ai-lombard

2. Создать виртуальное окружение

python -m venv .venv

3. Активировать виртуальное окружение

Для Windows:


.venv\Scripts\activate

Для macOS / Linux:


source .venv/bin/activate

4. Установить зависимости

pip install -r requirements.txt

5. Создать файл .env

В корне проекта создайте файл .env:

YC_API_KEY=your_yandex_cloud_api_key
YC_FOLDER_ID=your_yandex_cloud_folder_id

Где:


YC_API_KEY — API-ключ для доступа к Yandex Cloud AI Studio;
YC_FOLDER_ID — идентификатор каталога Yandex Cloud.

6. Запустить приложение

streamlit run app.py

После запуска приложение будет доступно в браузере по адресу:


http://localhost:8501

Пример пользовательского сценария

1.
Пользователь открывает веб-приложение.
2.
Принимает предупреждение о предварительном характере оценки.
3.
Загружает фотографии изделия.
4.
Указывает:
тип изделия;
пробу металла;
состояние;
наличие вставок;
вес, если известен.
5.
Нажимает кнопку «Оценить изделие».
6.
Получает предварительный расчёт:
вес изделия;
цену за грамм;
сумму займа;
сумму выкупа;
вероятность принятия;
замечания по дефектам.
7.
Выбирает действие:
«Выбрать подразделение»;
«Заказать обратный звонок».
8.
Заполняет ФИО и номер телефона.
9.
Сервис создаёт заявку через заглушку интеграции с Битрикс24.

Интеграция с Битрикс24

В MVP интеграция реализована в виде заглушки в файле bitrix.py.


При создании заявки формируется payload, который имитирует данные для отправки в Битрикс24:


ФИО клиента;
номер телефона;
тип изделия;
наличие вставок;
состояние изделия;
описание дефектов;
серьёзность дефектов;
вес изделия;
источник веса;
цена за грамм;
сумма займа;
сумма выкупа;
вероятность принятия;
количество загруженных фотографий.

В реальной версии вместо заглушки должен использоваться HTTP-запрос к REST API Битрикс24:


POST https://<портал>.bitrix24.ru/rest/<user_id>/<token>/crm.deal.add

Расчёт стоимости

Расчёт выполняется на основании внутренних тарифных сеток, заданных в pricing.py.


Для каждой пробы золота указана стоимость за грамм по категориям состояния:


cat1 — изделие в хорошем состоянии;
cat2 — изделие в среднем состоянии;
cat3 — изделие в плохом состоянии.

Поддерживаемые пробы:


375;
585;
750.

Внешние источники цен в MVP не используются.


Расчёт вероятности принятия

Вероятность принятия изделия рассчитывается эвристически на основании:


состояния изделия;
наличия дефектов;
серьёзности дефектов;
ремонтопригодности;
повреждения вставок;
наличия вставок;
указанного или оценочного веса.

Результат отображается в виде одного из вариантов:


высокая вероятность принятия;
средняя вероятность принятия;
низкая вероятность принятия;
приём маловероятен.

Ограничения MVP

Сервис предназначен только для предварительной оценки.
Вес по фотографии определяется приблизительно.
Итоговая сумма может отличаться после очного осмотра.
Интеграция с Битрикс24 реализована как заглушка.
Поддерживаются только золотые ювелирные изделия.
Тарифные сетки заданы локально в коде.
Карта подразделений в MVP заменена списком подразделений.

Команда

Название команды: airou


Участник: Ислибаев Родион Станиславович


разработка приложения;
настройка ИИ-анализа;
реализация расчёта стоимости;
реализация Streamlit-интерфейса;
подготовка интеграции с Битрикс24;
оформление проекта.

Контактные данные

Email: airouu.kai@gmail.com
Telegram: @airouuu

Статус проекта

MVP / прототип для хакатона.


Проект демонстрирует основной пользовательский сценарий: от загрузки фотографий изделия до получения предварительной оценки и формирования обращения.